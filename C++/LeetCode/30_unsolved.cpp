#include <iostream>
#include <vector>

using namespace std;

uint64_t getHashSumOfNSymbs(string s, int len)
{
    uint64_t result = 0;
    for(int i = 0; i < s.size(); i += len)
    
}

vector<int> findSubstring(string s, vector<string>& words)
{
    if(s.size() < words.size() * words[0].size())
        return {};
    vector<uint64_t> hash(words.size()+1), X(words.size()+1);
    int p = 1'000'000'007;
}

int main()
{
    string s = "barfoothefoobarman"
    return 0;
}